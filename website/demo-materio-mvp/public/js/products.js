// products.js — Fallback (catalog now loaded dynamically from /api/catalog)
// The real catalog is scraped from materio.ca → data/catalog-materio.json
// MAT_PRODUCTS is declared in app.js and populated from /api/catalog
